# 🔧 Настройка переменных окружения на Vercel

## ⚠️ ВАЖНО: Без этих переменных сайт не будет работать!

После деплоя на Vercel необходимо настроить переменные окружения для Supabase.

## 📝 Шаги настройки:

### 1. Откройте Vercel Dashboard
Перейдите на: https://vercel.com/dashboard

### 2. Выберите ваш проект
Найдите проект `react-pet-setting`

### 3. Откройте Settings → Environment Variables

### 4. Добавьте следующие переменные:

#### Для Supabase:
```
REACT_APP_SUPABASE_URL=https://ваш-проект.supabase.co
REACT_APP_SUPABASE_ANON_KEY=ваш-anon-key
```

#### Где найти эти значения:
1. Откройте Supabase Dashboard: https://app.supabase.com
2. Выберите ваш проект
3. Перейдите в **Settings** → **API**
4. Скопируйте:
   - **Project URL** → это `REACT_APP_SUPABASE_URL`
   - **anon public** key → это `REACT_APP_SUPABASE_ANON_KEY`

### 5. Выберите окружения:
- ✅ **Production**
- ✅ **Preview** (опционально)
- ✅ **Development** (опционально)

### 6. Сохраните и перезапустите деплой

После добавления переменных:
1. Нажмите **Save**
2. Перейдите в **Deployments**
3. Найдите последний деплой
4. Нажмите **⋮** (три точки) → **Redeploy**

Или просто сделайте новый commit и push - Vercel автоматически перезапустит деплой.

## 🔍 Проверка:

После перезапуска откройте сайт и проверьте консоль браузера (F12):
- ✅ Не должно быть ошибок о Supabase credentials
- ✅ Данные должны загружаться из Supabase

## 🐛 Если что-то не работает:

1. Проверьте, что переменные добавлены правильно (без пробелов, без кавычек)
2. Убедитесь, что выбраны правильные окружения (Production)
3. Проверьте, что деплой перезапущен после добавления переменных
4. Проверьте консоль браузера на ошибки

## 📞 Дополнительная помощь:

Если проблемы остаются:
- Проверьте логи деплоя в Vercel Dashboard → Deployments → [ваш деплой] → Logs
- Проверьте консоль браузера (F12) на ошибки
- Убедитесь, что Supabase проект активен и база данных настроена






