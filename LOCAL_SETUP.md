# 🚀 Локальный запуск проекта

## Установка зависимостей

Если еще не установлены:
```bash
npm install
```

## Настройка переменных окружения

Создайте файл `.env` в корне проекта `petcare-react/`:

```env
REACT_APP_SUPABASE_URL=https://ваш-проект.supabase.co
REACT_APP_SUPABASE_ANON_KEY=ваш-anon-key
REACT_APP_ADMIN_PASSWORD=admin123
```

**Где найти значения Supabase:**
1. Откройте https://app.supabase.com
2. Выберите ваш проект
3. Settings → API
4. Скопируйте **Project URL** и **anon public** key

## Запуск проекта

```bash
npm start
```

Приложение откроется автоматически в браузере по адресу: http://localhost:3000

## Возможные проблемы

### 1. Порт 3000 занят

Если порт занят, React автоматически предложит использовать другой порт (например, 3001).

Или убейте процесс на порту 3000:
```bash
# Windows PowerShell
Get-Process -Id (Get-NetTCPConnection -LocalPort 3000).OwningProcess | Stop-Process -Force

# Или просто используйте другой порт
set PORT=3001
npm start
```

### 2. Ошибки компиляции

Если видите ошибки компиляции:
- Проверьте консоль терминала
- Проверьте консоль браузера (F12)
- Убедитесь, что все зависимости установлены: `npm install`

### 3. Ошибки подключения к Supabase

Если видите ошибки о Supabase:
- Проверьте файл `.env` - переменные должны быть без пробелов и кавычек
- Убедитесь, что файл называется именно `.env` (не `.env.example`)
- Перезапустите dev сервер после создания/изменения `.env`

### 4. Приложение не открывается автоматически

Откройте вручную: http://localhost:3000

## Проверка работы

1. ✅ Главная страница открывается: http://localhost:3000
2. ✅ Страница услуг: http://localhost:3000/services
3. ✅ Страница бронирования: http://localhost:3000/booking
4. ✅ Админ панель: http://localhost:3000/admin (пароль: admin123)

## Отладка

### Проверка консоли браузера
1. Откройте браузер
2. Нажмите F12 (DevTools)
3. Перейдите на вкладку Console
4. Проверьте наличие ошибок

### Проверка консоли терминала
Все ошибки компиляции будут показаны в терминале, где запущен `npm start`.

## Если ничего не помогает

1. Удалите `node_modules` и `package-lock.json`:
```bash
rm -rf node_modules package-lock.json
# или в Windows PowerShell:
Remove-Item -Recurse -Force node_modules, package-lock.json
```

2. Переустановите зависимости:
```bash
npm install
```

3. Очистите кеш:
```bash
npm start -- --reset-cache
```



